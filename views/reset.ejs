<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Password - Data Fortress</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="flex items-center justify-center min-h-screen bg-gray-100">
    <div class="w-full max-w-md p-6 bg-white shadow-md rounded-lg">
      <h1 class="text-xl font-semibold text-gray-800 text-center mb-4">
        Reset your password
      </h1>

      <form action="/reset-password" method="POST" class="space-y-4">
        <div>
          <label for="password" class="block text-sm font-medium text-gray-700"
            >Enter New Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full p-2 mt-1 border rounded-md focus:ring-2 focus:ring-blue-500 focus:outline-none"
          />
        </div>

        <input type="hidden" name="token" value="<%= token %>" />

        <button
          type="submit"
          class="w-full px-4 py-2 text-white bg-blue-600 rounded-md hover:bg-blue-700 focus:outline-none"
        >
          Reset Password
        </button>
      </form>
    </div>
  </body>
</html>
